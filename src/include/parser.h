#ifndef PARSER_H
#define PARSER_H

#include <design1.h>

/* tree_code_t *build_tree() :
 * Builds the parse tree after the
 * lexer has been setup to the right
 * input
 */
tree_code_t *build_tree();

#endif
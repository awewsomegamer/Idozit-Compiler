#include <stdio.h>
#include <stdint.h>

extern uint64_t do_the_float();

int main() {
        // uint64_t *add = (uint64_t*)do_the_float();
        // for (int i = 0; i < 20; i++)
        //         printf("%02X ", *(add + i));
        // printf("\n");
}